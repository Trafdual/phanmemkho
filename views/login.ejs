<!DOCTYPE html>
<html>

<head>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.3/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script>
    <style>
        /* Your CSS styles here */
        body {
            font-family: Arial, sans-serif;
            background-color: #f2f2f2;
        }

        .login-container {
            width: 300px;
            margin: 0 auto;
            margin-top: 100px;
            background: #fff;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            text-align: center;
            /* Align the content in the center */
        }

        .logo {
            margin-bottom: 20px;
            background-color: #2FF2FE;
            border-radius: 5px;
            padding: 0px;
            /* Adjust the margin as needed */
        }


        input[type="text"],
        input[type="password"] {
            width: calc(100% - 20px);
            padding: 10px;
            margin-bottom: 15px;
            border: 1px solid #ddd;
            border-radius: 5px;
        }

        input[type="submit"] {
            width: 100%;
            padding: 10px;
            border: 0;
            background: #2FF2FE;
            color: #fff;
            border-radius: 5px;
            cursor: pointer;
        }

        input[type="submit"]:hover {
            background: #2c707f;
        }
    </style>
    <script>
        function togglePassword() {
            var x = document.getElementById("password");
            if (x.type === "password") {
                x.type = "text";
            } else {
                x.type = "password";
            }
        }
        function showErrorDialog() {
            var roleError = "<%= typeof RoleError !== 'undefined' ? RoleError : '' %>";
            if (roleError) {
                alert(roleError);
            }
        }
    </script>
</head>

<body onload="showErrorDialog()">

    <div class="login-container">
        <div class="logo">
            <h3 style="font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; color: red; font-weight: bold; font-size: 40px; text-shadow: 1px 2px 5px rgba(242, 11, 11, 0.5);">BICRAFT</h3>
        </div>
        <h2>Login</h2>
        <form name="loginForm" action="/loginadmin" method="post">
            <input type="text" placeholder="Email" id="email" name="email" required><br>
            <% if (typeof UserError !== 'undefined' && UserError) { %>
            <p class="alert alert-danger"><%= UserError %></p>
            <% } %>
            <input type="password" placeholder="Password" id="password" name="password" required><br>
            <% if (typeof PassError !== 'undefined' && PassError) { %>
            <p class="alert alert-danger"><%= PassError %></p>
            <% } %>
            <input type="checkbox" onclick="togglePassword()"> Hiển thị mật khẩu<br><br>
            <input type="submit" value="Login">
            <p style="margin-top:20px;">Bạn chưa có tài khoản? <a href="/getregister">Đăng kí</a></p>
        </form>
    </div>

</body>

</html>